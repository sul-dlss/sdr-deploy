version: 2.1

orbs:
  ruby: circleci/ruby@1.2.0

jobs:
  lint:
    docker:
      - image: cimg/ruby:2.7.3
    executor: ruby/default
    steps:
      - checkout
      - run:
          name: Which bundler?
          command: bundle -v
      - ruby/install-deps
      - ruby/rubocop-check

workflows:
  test:
    jobs:
      - lint
